import React from 'react';
import AlertSpeed from './AlertSpeed';
import AlertZone from './AlertZone';

const Alert = () => {
  return (
    <div>
      <AlertSpeed />
      <AlertZone />
    </div>
  );
};

export default Alert;
